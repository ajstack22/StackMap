name: Deploy to Staging (Qual)

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy to Qual via SSH
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: 199.188.200.57
        username: stachblx
        port: 21098
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          cd ~/public_html/qual
          git pull origin main
          echo "✅ Deployed to qual at $(date)"