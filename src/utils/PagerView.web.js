/**
 * Web stub for react-native-pager-view
 * This prevents build errors on web where PagerView is not used
 */

import { View } from 'react-native-web';

// Return a simple View component as PagerView is Android-only
export default View;
