# Pending Changes

## Title: Direct sync join from invite URLs

### Changes Made:
- Sync invite URLs now bypass the onboarding welcome screen
- Go directly to "Join Sync" screen with pre-filled invite code
- Initialize onboarding state correctly for sync invites on first render
- Auto-trigger sync preview when both invite code and recovery phrase are present
- Users see the sync import screen immediately without any intermediate steps

