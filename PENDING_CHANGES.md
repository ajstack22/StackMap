# Pending Changes

## Title: Simplified Sync Key UI with Clear Copy Actions

### Changes Made:
- Redesigned sync UI to treat invite code + recovery phrase as a single "Sync Key"
- Added "Generate Sync Key" button that shows the full key in a text box
- Added separate "Copy Key" and "Copy URL" buttons for clarity
- Key is displayed in a readable format with proper visual hierarchy
- Added "Generate New Key" option to refresh the temporary invite code
- Moved raw recovery phrase to advanced section for power users
- Fixed issue where generated sync key wasn't visible after creation

